# Local Firebase SDK

Firebase JS SDK 12.19.0, bundled from the official npm firebase package.
Rebuild with npm ci followed by npm run build:firebase.
License notices: firebase.js.LEGAL.txt. No CDN is required at runtime.
